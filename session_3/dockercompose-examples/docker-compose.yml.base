version: '3'

services:
  db:
    image: 'postgres'
    ports:
      - '5432:5432'
    volumes:
      - 'postgres:/data'

  web:
    build: .
    depends_on:
      - 'db'
    env_file:
      - '.env'
    ports:
      - '8000:3000'
    volumes:
      - '.:/app'

volumes:
  redis: {}